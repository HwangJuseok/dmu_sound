<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DMU Sound - Home</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- 필요시 추가 -->
</head>
<body>
<header>
    <h1>🎵 DMU Sound</h1>
</header>

<main>
    <form action="/search" method="get" class="search-form">
        <input type="text" name="query" placeholder="노래, 아티스트 검색" required>
        <button type="submit">검색</button>
    </form>

    <!-- Spotify 신곡 섹션 -->
    <section class="spotify-section">
        <h2>📀 신곡 추천 (Spotify)</h2>
        <div class="release-list">
            {{#newReleases}}
                <div class="release-item">
                    <img src="{{imageUrl}}" alt="{{albumName}}" class="release-image">
                    <p><strong>{{albumName}}</strong></p>
                    <p>{{artistName}}</p>
                </div>
            {{/newReleases}}
            {{^newReleases}}
                <p class="no-data">불러올 신곡이 없습니다.</p>
            {{/newReleases}}
        </div>
    </section>

    <!-- YouTube 인기 음악 섹션 -->
    <section class="youtube-section">
        <h2>🔥 인기 음악 (YouTube)</h2>
        <div class="video-list">
            {{#trendingVideos}}
                <div class="video-item">
                    <img src="{{thumbnailUrl}}" alt="{{title}}" class="video-thumbnail">
                    <p><strong>{{title}}</strong></p>
                    <p>{{channel}}</p>
                </div>
            {{/trendingVideos}}
            {{^trendingVideos}}
                <p class="no-data">인기 음악 정보를 불러올 수 없습니다.</p>
            {{/trendingVideos}}
        </div>
    </section>
</main>
</body>
</html>